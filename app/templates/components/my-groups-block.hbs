<div class="grid-block">
  <div class="grid-block__title">Mijn groepen</div>
  <div class="groups-block">
    {{#each this.memberships as |membership|}}
    <div class="group {{if showGroupMembers "group--expanded"}}">
      <div class="group__name">{{membership.studentgroup.groupName}}</div>
      <i
        class="group__expand icon-arrow"
        role="button"
        {{ action "expandGroup" }}
      >
      </i>
      {{# if showGroupMembers }}
        <div class="group__members">
          {{#each membership.studentgroup.memberships as |member|}}
          <div class="group__members__member">
            <i class="icon-user"></i>
            <span class="member__name">{{member.user.username}}</span>
          </div>
          {{/each}}
        </div>
      {{/if}}
    </div>
    {{/each}}
  </div>
</div>
